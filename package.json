{
  "name": "@subtle-js/auth",
  "version": "0.0.3",
  "description": "The `Auth` interface provides a set of methods to perform authentication operations.",
  "main": "dist/index.js",
  "types": "types/index.d.ts",
  "files": [
    "dist",
    "types"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rimraf dist types",
    "build": "run-s build:*",
    "build:tsc": "tsc --outDir dist",
    "build:declaration": "tsc --outDir types --declaration --emitDeclarationOnly"
  },
  "keywords": [
    "subtle-js",
    "auth"
  ],
  "author": "@subtle-js <https://github.com/subtle-js>",
  "repository": {
    "type": "git",
    "url": "https://github.com/subtle-js/auth.git"
  },
  "license": "MIT",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.4"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  }
}
